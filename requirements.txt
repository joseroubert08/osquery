Jinja2
argparse
thrift

# The pip / setup tools for Windows will not include psutil
# Needed for integration tests
psutil
pexpect

# For building the wiki
mkdocs
setuptools>=65.5.1 # not directly required, pinned by Snyk to avoid a vulnerability
